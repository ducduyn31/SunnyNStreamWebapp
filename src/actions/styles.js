import { createActions } from 'redux-actions';
import { ActionTypes } from '../constants';

export const {} = createActions({
  [ActionTypes.HIDE_APP_BAR]: () => ({}),
  [ActionTypes.SHOW_APP_BAR]: () => ({}),
  [ActionTypes.TRANSPARENT_APP_BAR]: () => ({}),
  [ActionTypes.MATERIAL_APP_BAR]: () => ({}),
});
